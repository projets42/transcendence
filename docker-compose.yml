services:
  nginx:
    container_name: ngx
    build: ./nginx
    restart: on-failure
    depends_on:
      - django
    ports:
      - "443:443"
    # volumes:
    #   - website:/var/www/html
    networks:
      - trscd
  django:
    container_name: django
    env_file: .env
    build: ./django
    restart: on-failure
    depends_on:
      - postgresql
    ports:
      - "8000:8000"
    # expose:
    #   - "8000"
    # volumes:
    #   - website:/var/www/html
    networks:
      - trscd
  postgresql:
    container_name: postgre
    build: ./postgresql
    restart: on-failure
    env_file: .env
    expose:
      - "5432"
    # volumes:
    #   - database:/var/lib/postgresql/data
    networks:
      - trscd
# volumes:
#   database:
#     driver: local
#     driver_opts:
#       type: 'none'
#       o: 'bind'
networks:
  trscd:
    driver: bridge
